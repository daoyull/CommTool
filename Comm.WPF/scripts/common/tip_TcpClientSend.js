const client = {

    /**
     * 发送数据
     * @param {string} message - 发送信息
     */
    send(message) {
    },

    /**
     * 发送数据
     * @param {Uint8Array} array - 发送信息
     */
    sendBuffer(array) {
    },

}