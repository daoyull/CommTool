<commonWpf:UserComponent
    x:Class="NetTool.Views.TcpClientView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:commonWpf="clr-namespace:CommonWpf;assembly=Common.WPF"
    xmlns:components="clr-namespace:NetTool.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels1="clr-namespace:NetTool.ViewModels"
    d:DataContext="{d:DesignData viewModels:TcpClientViewModel}"
    d:DesignHeight="600"
    d:DesignWidth="1000"
    x:TypeArguments="viewModels1:TcpClientViewModel"
    Background="White"
    mc:Ignorable="d">
    <commonWpf:UserComponent.Resources>
        <ResourceDictionary>
            <Style x:Key="ReceiveRichTextBox" TargetType="components:NetLogger">
                <Setter Property="BorderBrush" Value="Gray" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="IsReadOnly" Value="True" />

            </Style>
        </ResourceDictionary>


    </commonWpf:UserComponent.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0">
            <GroupBox Margin="16" Header="连接设置">
                <StackPanel>
                    <TextBox
                        Width="100"
                        Margin="0,5,0,0"
                        materialDesign:HintAssist.Hint="IP"
                        Style="{StaticResource MaterialDesignFilledTextBox}"
                        Text="{Binding Config.Ip}" />
                    <TextBox
                        Width="100"
                        Margin="0,5,0,0"
                        materialDesign:HintAssist.Hint="Port"
                        Style="{StaticResource MaterialDesignFilledTextBox}"
                        Text="{Binding Config.Port}" />
                    <Button Margin="0,5,0,0" Command="{Binding ConnectionCommand}">连接</Button>
                </StackPanel>
            </GroupBox>

        </StackPanel>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="9*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="3*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Padding="5"
                    BorderBrush="Gray"
                    BorderThickness="1"
                    CornerRadius="5">
                    <components:NetLogger
                        x:Name="NetLogger" Style="{DynamicResource ReceiveRichTextBox}" />
            </Border>

            <StackPanel Grid.Row="1">
                <Border Height="2" Background="Black" />
            </StackPanel>
            <Grid x:Name="SendGrid" Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="9*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <RichTextBox Grid.Column="0" MinHeight="100" />
                <Button
                    Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch">
                    发送
                </Button>
            </Grid>
        </Grid>
    </Grid>
</commonWpf:UserComponent>